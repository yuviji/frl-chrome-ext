Replayer Python package.

Usage:

```
python -m replayer.replay path/to/trace.json --headed
```

Note: Install browser binaries once after installing dependencies:

```
python -m playwright install chromium
```
